<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:id="@+id/detailMovieActivityTextViewFooter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/neosans"
        android:text="@string/similar_message"
        android:textColor="@color/colorPrimary"
        android:layout_margin="16dp"
        android:textSize="16sp"
        android:textStyle="bold" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/detailMovieActivityRecyclerViewFooter"
        android:layout_below="@id/detailMovieActivityTextViewFooter"
        android:clipToPadding="false"
        android:paddingStart="16dp"
        android:paddingEnd="0dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <ProgressBar
        android:id="@+id/detailMovieActivityProgressDialogFooter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/detailMovieActivityTextViewFooter"
        android:layout_centerHorizontal="true"/>
</RelativeLayout>